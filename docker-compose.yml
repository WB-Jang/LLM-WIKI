services:
  wiki:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./wiki:/app/wiki
      - ./raw:/app/raw
      - ./config.yaml:/app/config.yaml
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
